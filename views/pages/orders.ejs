<!DOCTYPE html>
<html>
<head>
  <title>Your Orders</title>
  <style>
    body {
  font-family: 'Segoe UI', sans-serif;
  background: radial-gradient(circle, #fff8f0 0%, #ffe6cc 40%, #ffd5b5 100%);
  margin: 0;
  padding-top: 80px;
}


    h3 {
      font-size: 20px;
      text-align: center;
      margin-bottom: 30px;
      color: #333;
      font-family:Georgia, 'Times New Roman', Times, serif
    }

    .order-card {
      background: #fff;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.08);
      margin: 20px auto;
      max-width: 1100px;
    }

    .order-header {
      display: flex;
      justify-content: space-between;
      border-bottom: 1px solid #eee;
      padding-bottom: 10px;
      margin-bottom: 15px;
    }

    .order-header span {
      font-size: 16px;
      color: #555;
    }

    .order-items {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
    }

    .item-card {
      display: flex;
      background: #fff8f0;
      border-radius: 10px;
      padding: 12px;
      width: calc(50% - 70px);
      align-items: center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.08);
    }

    .item-card img {
      width: 90px;
      height: 90px;
      border-radius: 8px;
      object-fit: cover;
      margin-right: 18px;
    }

    .item-details {
      flex: 1;
    }

    .item-details h4 {
      margin: 0;
      font-size: 18px;
      color: #333;
    }

    .item-details p {
      margin: 6px 0;
      font-size: 15px;
      color: #555;
    }

    .total-amount {
      text-align: right;
      margin-top: 15px;
      font-size: 18px;
      font-weight: 600;
      color: #222;
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 30px;
      text-decoration: none;
      font-size: 18px;
      color: #007185;
    }
     .navbar {
  display: flex;
  align-items: center;
  background-color: #fff8f0;
  padding: 10px 20px;
  box-shadow: 0 3px 8px rgba(0, 0, 0, 0.08);
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 20;
  height: 70px;
}

/* Left section */
.nav-left {
  display: flex;
  align-items: center;
}

.nav-left .logoimg {
  width: 50px;
  height: 45px;
  margin-right: 8px;
}

.nav-left .logo {
  font-size: 30px;
  font-family: 'Cambria', serif;
  color: #e07b39;
}

/* Center section (nav links) */
.nav-center {
  display: flex;
  align-items: center;
  margin: 0 auto;
}

.nav-center a {
  text-decoration: none;
  color: #333;
  margin: 0 20px;
  font-size: 18px;
  transition: color 0.3s ease;
}

.nav-center a:hover {
  color: #e07b39;
}

/* Dropdown adjustments */
.nav-center .dropdown {
  position: relative;
}

.nav-center .dropdown-content {
  display: none;
  position: absolute;
  background-color: #fffaf2;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.12);
  border-radius: 6px;
  padding: 10px 0;
  min-width: 150px;
  top: 30px;
  left: 0;
}

.nav-center .dropdown-content div {
  padding: 12px 20px;
  font-size: 16px;
  color: #444;
  cursor: pointer;
}

.nav-center .dropdown-content div:hover {
  background-color: #ffe0c2;
}

.nav-center .dropdown:hover .dropdown-content {
  display: block;
}

/* Right section */
.nav-right {
  display: flex;
  align-items: center;
  margin-left: auto;
}

.nav-right a img {
  width: 34px;
  height: 34px;
  margin-left: 16px;
  transition: transform 0.2s ease;
}

.nav-right a img:hover {
  transform: scale(1.15);
}
    .nav-right .dropdown {
  position: relative;
  display: inline-block;
}

.nav-right .dropdown-content {
  display: none;
  position: absolute;
  background-color: #fffaf2;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.12);
  border-radius: 6px;
  padding: 10px 0;
  min-width: 150px;
  top: 40px;
  right: 0;
  z-index: 25;
}

.nav-right .dropdown-content div {
  padding: 12px 20px;
  font-size: 16px;
  color: #444;
  cursor: pointer;
}

.nav-right .dropdown-content div:hover {
  background-color: #ffe0c2;
}

.nav-right .dropdown:hover .dropdown-content {
  display: block;
}
  </style>
</head>
<body>
  <div class="navbar">
  <div class="nav-left">
    <a href="/"><img src="images/logo.png" class="logoimg"></a>
    <a href="/" style="text-decoration: none;color: inherit;"><p class="logo"><b>WOOD <span style="color:black">STORE</span></b></p></a>
  </div>

  <div class="nav-center">
    <a href="/">HOME</a>
    <a href="/about">ABOUT</a>
    <a href="/contact">CONTACT</a>
    <div class="dropdown">
      <a href="#">CATEGORIES ▼</a>
      <div class="dropdown-content">
        <a href="/category/4521">Bed</a>
        <a href="/category/3648">Dining</a>
        <a href="/category/1831">Living</a>
        <a href="/category/7895">Study</a>
      </div>
    </div>
  </div>

  <div class="nav-right">
   <div class="dropdown">
    <a href="#"><img src="/images/login1.png" alt="login"></a>
    <div class="dropdown-content" style="right:0; left:auto; min-width:160px;">
      <div onclick="location.href='/login'">Profile Details</div>
      <div onclick="location.href='/orders'">Order History</div>
  </div>
</div>
  </div>
</div>
<h3>ORDER HISTORY</h3>

<% if (orders.length === 0) { %>
  <p style="text-align:center; font-size:20px;">No Orders Found!</p>
<% } else { %>

  <% 
  var groupedOrders = {};
  orders.forEach(function(o) {
    if (!groupedOrders[o.order_id]) groupedOrders[o.order_id] = [];
    groupedOrders[o.order_id].push(o);
  });

  Object.keys(groupedOrders).sort((a, b) => b - a).forEach(function(orderId) {
%>
  <div class="order-card">
    <div class="order-header">
      <span>Order ID: <%= orderId %></span>
      <span>Placed on: <%= new Date(groupedOrders[orderId][0].date).toLocaleString() %></span>
    </div>

    <div class="order-items">
      <% groupedOrders[orderId].forEach(function(item) { %>
        <div class="item-card">
          <img src="/images/<%= item.img_path %>" alt="<%= item.pname %>">
          <div class="item-details">
            <h4><%= item.pname %></h4>
            <p>Qty: <%= item.qty %></p>
            <p>Price: <%= formatCurrency(item.pcost) %></p>
          </div>
        </div>
      <% }); %>
    </div>

    <div class="total-amount">
      Order Total: <%= formatCurrency(groupedOrders[orderId][0].totalamt) %>
    </div>
  </div>
<% }); %>

<% } %>

<a href="/website" class="back-link">← Back to Home</a>
<%- include('footer') %>

</body>
</html>
