<!DOCTYPE html>
<html>
<head>
  <title>Order Placed</title>
  <style>
     body {
      font-family: 'Segoe UI', sans-serif;
      background: radial-gradient(circle, #fff8f0 0%, #ffe6cc 40%, #ffd5b5 100%);
      margin: 0;
    }

    /* Navbar Styles */
    .navbar {
      display: flex;
      align-items: center;
      background-color: #fff8f0;
      padding: 10px 20px;
      box-shadow: 0 3px 8px rgba(0, 0, 0, 0.08);
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 20;
      height: 70px;
    }
    .nav-left {
      display: flex;
      align-items: center;
    }
    .nav-left .logoimg {
      width: 50px;
      height: 45px;
      margin-right: 8px;
    }
    .nav-left .logo {
      font-size: 30px;
      font-family: 'Cambria', serif;
      color: #e07b39;
    }
    .nav-center {
      display: flex;
      align-items: center;
      margin: 0 auto;
    }
    .nav-center a {
      text-decoration: none;
      color: #333;
      margin: 0 20px;
      font-size: 18px;
      transition: color 0.3s ease;
      font-weight: normal;
    }
    .nav-center a:hover {
      color: #e07b39;
    }
    .dropdown {
      position: relative;
    }
    /* Dropdown adjustments */
.nav-center .dropdown {
  position: relative;
}

.nav-center .dropdown-content {
  display: none;
  position: absolute;
  background-color: #fffaf2;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.12);
  border-radius: 6px;
  padding: 10px 0;
  min-width: 150px;
  top: 30px;
  left: 0;
}

.nav-center .dropdown-content div {
  padding: 12px 20px;
  font-size: 16px;
  color: #444;
  cursor: pointer;
}

.nav-center .dropdown-content div:hover {
  background-color: #ffe0c2;
}

.nav-center .dropdown:hover .dropdown-content {
  display: block;
}
    .nav-right a img {
      width: 36px;
      margin-left: 18px;
      vertical-align: middle;
    }
    .nav-right a img:hover {
      transform: scale(1.15);
    }

    .nav-right .dropdown {
  position: relative;
  display: inline-block;
}

.nav-right .dropdown-content {
  display: none;
  position: absolute;
  background-color: #fffaf2;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.12);
  border-radius: 6px;
  padding: 10px 0;
  min-width: 150px;
  top: 40px;
  right: 0;
  z-index: 25;
}

.nav-right .dropdown-content div {
  padding: 12px 20px;
  font-size: 16px;
  color: #444;
  cursor: pointer;
}

.nav-right .dropdown-content div:hover {
  background-color: #ffe0c2;
}

.nav-right .dropdown:hover .dropdown-content {
  display: block;
}

  </style>
</head>
<body>
<!-- Navbar -->
<div class="navbar">
  <div class="nav-left">
    <a href="/"><img src="images/logo.png" class="logoimg"></a>
    <a href="/" style="text-decoration: none;color: inherit;"><p class="logo"><b>WOOD <span style="color:black">STORE</span></b></p></a>
  </div>

  <div class="nav-center">
    <a href="/">HOME</a>
    <a href="/about">ABOUT</a>
    <a href="/contact">CONTACT</a>
    <div class="dropdown">
      <a href="#">CATEGORIES ▼</a>
      <div class="dropdown-content">
        <a href="/category/4521">Bed</a>
        <a href="/category/3648">Dining</a>
        <a href="/category/1831">Living</a>
        <a href="/category/7895">Study</a>
      </div>
    </div>
  </div>

  <div class="nav-right">
   <div class="dropdown">
    <a href="#"><img src="/images/login1.png" alt="login"></a>
    <div class="dropdown-content" style="right:0; left:auto; min-width:160px;">
      <div onclick="location.href='/login'">Profile Details</div>
      <div onclick="location.href='/orders'">Order History</div>
  </div>
</div>
</div>
</div>

<h3 style="text-align:center; margin-top:150px">Order Placed Successfully!</h3>

<div class="order-card" style="max-width: 900px; margin-top:20px; margin-left:270px; padding: 20px; background: #ffffff51; border-radius: 10px; box-shadow: 0 4px 8px rgba(0,0,0,0.08);">
  <div class="order-header" style="display: flex; justify-content: space-between; border-bottom: 1px solid #ddd; margin-bottom: 15px; padding-bottom: 10px;">
    <span><strong>Order ID:</strong> <%= orderId %></span>
    <span><strong>Placed on:</strong> <%= new Date(orderDate).toLocaleString() %></span>
  </div>

  <% items.forEach(item => { %>
    <div style="display: flex; margin-bottom: 15px;">
      <img src="/images/<%= item.img_path %>" alt="<%= item.pname %>" style="width: 90px; height: 90px; object-fit: cover; border-radius: 8px; margin-right: 15px;">
      <div>
        <p><strong><%= item.pname.toUpperCase() %></strong></p>
        <p>Qty: <%= item.quantity %>  Price: <%= formatCurrency(item.pcost) %></p>
        <p>Order Total: <%= formatCurrency(item.pcost * item.quantity) %></p>
      </div>
    </div>
  <% }) %>

  <div style="text-align: right; font-weight: 600; margin-top: 20px;">
    Total: ₹<%= total %>
  </div>
</div>

<a href="/website" style="display:block; text-align:center; margin:30px 0; font-size:18px; text-decoration:none; color:#007185;">← Back to Home</a>
</body>
</html>
